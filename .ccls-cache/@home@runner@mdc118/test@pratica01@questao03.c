#include <stdio.h>
#include <math.h>

int main() {
  /*entrada*/
#define PI 3.1416
  float r = 0.0f;
  printf("Entre aqui com o valor maior que 0 de r: ");
  scanf("%f", &r);
  /*processamento*/
float P = (P = 2 * PI * r);
  /*saida*/
  printf("O valor do perimetro é %.f\n", P);
  return 0;
}