#include <stdio.h>

int main() {
   /*entrada*/
float base = 0.0f;
  float altura = 0.0f;
  printf("Entre aqui com o valor maior que 0 da base: ");
  scanf("%f", &base);
  printf("Entre aqui com um valor maior que 0 da altura: ");
  scanf("%f", &altura);
  /*processamento*/
  float a = (a = base * altura / 2);
  /*saida*/
  printf("O valor de a é %.f\n", a);
  return 0;
}